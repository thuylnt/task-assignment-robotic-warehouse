Hướng dẫn chi tiết chuẩn bị cho epymarl

cd "E:\Khoa hoc\USTH\Machine Learning\Source\task-assignment-robotic-warehouse"                                                                            
.venv\Scripts\activate   
cd "E:\Khoa hoc\USTH\Machine Learning\Source\epymarl"                                                                                                        
pip install -r requirements.txt    
   
# Torch cài sẵn từ PyPI là bản CPU-only trên Windows (PyPI mặc định thế). Cần gỡ ra và cài từ pytorch index.                                                 
pip uninstall -y torch torchvision 
pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
Nếu cu128 báo không có wheel (401/404), thử các bản cũ hơn theo thứ tự:                                 
pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124                                                                           
pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121

# Verify
python -c "import torch; print('Torch:', torch.__version__); print('CUDA avail:', torch.cuda.is_available()); print('Device:',  torch.cuda.get_device_name(0) if torch.cuda.is_available() else 'CPU only')"

Torch: 2.11.0+cu128
CUDA avail: True
Device: NVIDIA GeForce RTX 5060 Ti

# Verify tarware vẫn hoạt động
(Chạy lại tarware)

pip install pettingzoo
pip install tensorboard  

Lệnh train có dạng như là
python src/main.py --config=ia2c --env-config=tarware with t_max=50000 use_tensorboard=True use_cuda=True             